{
  "name": "sprite-forge-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri shell for SPRITE//FORGE",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
