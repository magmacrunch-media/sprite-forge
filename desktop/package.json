{
  "name": "sprite-forge-desktop",
  "version": "0.3.0",
  "private": true,
  "description": "Tauri shell for SPRITE//FORGE",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "_comment": "Pinned EXACTLY, and to the same version magma-ops-app pins. app/kit/bridge-core.js subscribes to events through transformCallback, a Tauri internal, so the shape must not shift under a patch bump — and both apps now ride the one copy of that code.",
    "@tauri-apps/cli": "2.9.2"
  }
}
